apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data-persisent-volume-claim
spec:
  volumeName: redis-data-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
