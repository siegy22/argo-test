apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synchronist-postgres-data-persisent-volume-claim
spec:
  volumeName: synchronist-postgres-data-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
